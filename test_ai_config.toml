# Test configuration for OpenAgent Terminal with AI enabled

[ai]
enabled = true
provider = "null"  # Use null provider for testing (won't make real API calls)
panel_height_fraction = 0.4
backdrop_alpha = 0.3

[colors]
# Use default colors

[ui]
# Use default UI settings
