[package]
name = "docker-helper"
version = "1.0.0"
edition = "2021"

[dependencies]
plugin-api = { path = "../../crates/plugin-api" }
serde_json = "1.0"
regex = "1.10"

[lib]
name = "docker_helper"
path = "src/lib.rs"
crate-type = ["cdylib"]

[features]
default = []
wasi = []
