[package]
name = "plugin-api"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[lib]
name = "plugin_api"
path = "src/lib.rs"
crate-type = ["rlib", "cdylib"]
